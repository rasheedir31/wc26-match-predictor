# Normalize line endings: LF in the repo, checked out native on each platform.
* text=auto eol=lf

# Binaries - never normalize.
*.png binary
*.jpg binary
*.parquet binary
*.pkl binary
*.joblib binary
*.zip binary
